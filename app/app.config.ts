export default defineAppConfig({
  ui: {
    colors: {
      // `ke` is the Kaduna Electric green defined in assets/css/main.css.
      primary: 'ke',
      secondary: 'ke-lime',
      neutral: 'zinc'
    },
    card: {
      slots: {
        root: 'rounded-xl'
      }
    }
  }
})
